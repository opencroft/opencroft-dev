{
  "name": "opencroft",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "check": "biome check",
    "fix": "biome check --write",
    "dev": "npm run dev -w @opencroft/app",
    "build": "npm run build -w @opencroft/app",
    "start": "npm run start -w @opencroft/app"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16"
  }
}
